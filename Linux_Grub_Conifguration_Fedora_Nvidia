This grub configuration file solves problem regarding Nvidia Graphics Cards and Feodra.

# Command to edit the GRUB Configuration file.
sudo nano /etc/default/grub


The file should contain the following:

/etc/default/grub file: GRUB_TIMEOUT=5
GRUB_DISTRIBUTOR="$(sed 's, release .*$,,g' /etc/system-release)"
GRUB_DEFAULT=saved
GRUB_DISABLE_SUBMENU=true
GRUB_TERMINAL_OUTPUT="console"
GRUB_CMDLINE_LINUX="rhgb quiet rd.driver.blacklist=nouveau modprobe.blacklist=nouveau nvidia-drm.modeset=1 iommu=pt"
GRUB_DISABLE_RECOVERY="true"
GRUB_ENABLE_BLSCFG=true

# Update GRUB
sudo grub2-mkconfig -o /boot/grub2/grub.cfg
